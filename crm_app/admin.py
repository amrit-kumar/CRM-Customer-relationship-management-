from django.contrib import admin

# # Register your models here.
# # from tixdo.crm.models import MsgReports
#
# class MsgReportsAdmin(admin.ModelAdmin):
#     list_display = ['id', 'request_id', 'user_id', 'date', 'number', 'discription','status','sender_id', 'campaign_name', 'status']
#     search_fields = ['id', 'request_id', 'user_id', 'date', 'number', 'discription','status','sender_id', 'campaign_name', 'status']
# admin.site.register(MsgReports, MsgReportsAdmin)
